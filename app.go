package main

/*import (
	"fyne.io/fyne/v2"
	"fyne.io/fyne/v2/app"
	"github.com/sagernet/sagerconnect/res"
)

var mainApp *MainApplication

type MainApplication struct {
	fyne.App
	window fyne.Window
}

func applicationInit() {
	mainApp = &MainApplication{
		App: app.New(),
	}
	mainApp.SetIcon(fyne.NewStaticResource("Icon.png", res.IconPng))
	mainApp.window = mainApp.NewWindow("SagerConnect")
	mainApp.initWindow()
	mainApp.window.Show()
}

func (w *MainApplication) initWindow() {
}
*/
